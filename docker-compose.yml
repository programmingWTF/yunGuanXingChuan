services:
  app:
    build: .
    container_name: yunguanxingchuan
    ports:
      - '8123:8000'
    volumes:
      - ./data:/app/data
      - ./.env:/app/.env:ro
    restart: unless-stopped
    environment:
      - TZ=Asia/Shanghai
