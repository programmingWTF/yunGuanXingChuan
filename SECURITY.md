# 安全政策（云观星传）

> 本项目是挑战杯揭榜挂帅擂台赛作品（XH-202619），公开仓库同样欢迎安全反馈。

## 报告安全问题的正确方式

**敏感安全问题（凭证泄露、越权、注入、数据泄露等）请不要公开讨论**，按以下方式报告：

1. **首选**：在本仓库开 Issue，标题带 `[安全]` 前缀，并打上标签
   - 🔒 **影响：安全** —— 涉及安全边界、凭证、权限、敏感数据
   - 请附上：影响范围、复现步骤（不含真实密钥/敏感数据）、建议修复方向
2. 如涉及**线上成品站**（yunguanxingchuan.xyz）的漏洞，请同时在 Issue 里说明，维护者会优先处理。

维护者承诺：收到安全相关 Issue 后 **24 小时内回复**，并在修复完成前不公开讨论细节。

## 红线（所有贡献者必读）

- **绝不提交密钥**：`.env`、`*.key`、`*.pem` 等已被 `.gitignore` 排除；任何 API Key 一律通过环境变量注入，禁止硬编码进代码或文档。
- **绝不在 Issue / PR / 评论里粘贴真实密钥**（包括脱敏以外的完整值）。
- **数据红线**：`data/` 下的用户数据、知识库语料属项目资产，提交前确认不含个人信息或敏感内容。

## 依赖与供应链

- 依赖锁定见 `requirements.txt`（后端）与 `frontend/package.json`（前端）。
- 发现依赖存在已知漏洞时，请开 Issue 打 `📦 依赖` 或 `🔒 影响：安全` 标签。

## 免责声明

本项目为竞赛作品，不构成生产级安全承诺；但维护者仍会认真对待每一条安全反馈。