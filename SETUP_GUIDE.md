# 📘 环境配置与 API Key 获取指南

> 写给不熟悉编程的同学：这份指南会手把手教你获取项目运行所需的 API Key。

---

## 🤔 什么是 API Key？

把 API Key 想象成一把**钥匙**：

- 你要用阿里云的通义千问大模型，就像去一个会员制图书馆借书
- **API Key 就是你的借书证**
- 有了它，程序才能调用 AI 服务
- 每个 Key 是**唯一的**、**私密的**——就像银行卡密码，不要告诉别人，更不要上传到公开的 GitHub

---

## 📋 你需要获取哪些 Key？

本项目需要以下 API Key：

| Key 名称 | 必须？ | 用途 | 获取难度 |
|----------|--------|------|---------|
| 阿里云百炼 API Key | ✅ 必须 | 调用通义千问大模型 | ⭐ 简单（免费开通） |
| Tavily API Key | ⭕ 推荐 | 搜索国际新闻（获取真实 URL） | ⭐ 简单（有免费额度） |

> 💡 **最小启动配置**：只需阿里云百炼 API Key 就能跑起来。

---

## 1️⃣ 获取阿里云百炼 API Key（必须）

阿里云百炼是阿里云的大模型服务平台，通义千问（Qwen）模型在上面运行。

### 第一步：注册/登录阿里云账号

1. 打开浏览器，访问：**https://bailian.console.aliyun.com/**
2. 如果你有淘宝/支付宝账号，可以直接扫码登录
3. 如果没有，点击"免费注册"，用手机号或邮箱注册
4. 注册后可能需要实名认证（上传身份证正反面，通常几分钟内审核通过）

### 第二步：进入百炼控制台

登录后，你会看到百炼的控制台页面。

### 第三步：开通模型服务

1. 在左侧菜单找到 **"模型广场"**
2. 搜索或找到 **"Qwen3"** 系列模型（如 qwen-plus、qwen-max 等）
3. 点击 **"开通服务"**（免费开通，按使用量付费，新用户有免费额度）

### 第四步：创建 API Key

1. 点击页面右上角的头像/个人中心
2. 选择 **"API-KEY 管理"**
   > 或直接访问：**https://bailian.console.aliyun.com/?apiKey=1**
3. 点击 **"创建新的 API Key"**
4. 给 Key 取个名字（比如"云观星传"）
5. 点击确定后，**立刻复制并保存**生成的 Key！

> ⚠️ **重要**：API Key 只显示一次！关闭弹窗后无法再次查看。请立即复制保存到安全的地方。

### 第五步：把 Key 填到项目里

打开项目中的 `.env` 文件，找到这一行：
```
QWEN_API_KEY=your_bailian_api_key_here
```

把 `your_bailian_api_key_here` 替换成你刚才复制的 Key，例如：
```
QWEN_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

同时把 `DASHSCOPE_API_KEY` 也填上同一个 Key（用于联网搜索）：
```
DASHSCOPE_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

### 💰 费用说明

- 阿里云百炼采用**按量计费**模式
- 新用户注册通常有**免费试用额度**（几十万到几百万 Token）
- 对于本项目的测试使用，免费额度完全够用
- Qwen3 系列模型价格大约 2-4 元/百万 Token（非常便宜）
- 查看余额：百炼控制台 → 账户 → 余额

---

## 2️⃣ 获取 Tavily API Key（推荐但非必须）

Tavily 是一个专为 AI Agent 设计的搜索引擎，可以搜索实时网页内容并返回结构化结果。

> 本项目用 Tavily 来搜索国际媒体对航天议题的报道。如果访问不了 Tavily，系统会自动回退使用百炼的联网搜索。

### 第一步：注册 Tavily 账号

1. 打开浏览器，访问：**https://app.tavily.com/**
2. 点击 **"Sign Up"** 或 **"Get Started"**
3. 可以用 Google 账号登录，或用邮箱注册
4. 不需要绑定信用卡

### 第二步：获取 API Key

1. 登录后，进入 **Dashboard**（控制面板）
2. 你会看到页面上显示你的 API Key
3. 点击 **复制** 按钮

### 第三步：把 Key 填到项目里

打开项目中的 `.env` 文件，找到这一行：
```
TAVILY_API_KEY=your_tavily_api_key_here
```

替换为你复制的 Key：
```
TAVILY_API_KEY=tvly-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

### 💰 费用说明

- Tavily 提供**免费的开发者计划**：每月 1000 次搜索
- 对于本项目的测试和学习使用，免费额度绰绰有余
- 如果免费额度用完，付费计划从 $20/月起步

---

## 📝 配置完后的检查清单

在运行项目之前，确认以下事项：

- [ ] `.env.example` 已复制为 `.env`
- [ ] `.env` 中的 `QWEN_API_KEY` 已填入真实 Key（不是 `your_bailian_api_key_here`）
- [ ] `.env` 中的 `DASHSCOPE_API_KEY` 已填入（可与 `QWEN_API_KEY` 相同）
- [ ] （可选）`.env` 中的 `TAVILY_API_KEY` 已填入
- [ ] Python 已安装（在终端运行 `python --version` 验证）
- [ ] Node.js 已安装（在终端运行 `node --version` 验证）
- [ ] 已运行 `pip install -r requirements.txt`

全部打勾后，就可以使用了。打开前端网页，在**任务中心**输入议题（如"嫦娥六号"）点击启动即可，全程不需要写代码（具体打开方式见 README 的「第四步：运行项目」）。

---

## 🔒 安全提醒

1. **绝对不要把 `.env` 文件提交到 GitHub！**（项目的 `.gitignore` 已经配置了忽略 `.env`）
2. **不要截图分享你的 API Key**
3. **不要在微信/QQ/邮件中明文发送 API Key**
4. 如果 API Key 不小心泄露，立即去对应的控制台**删除并重新创建**
5. 每个团队成员应该使用**自己**的 API Key，不要共用

---

## 🆘 常见问题

### Q：我没有阿里云账号，也没有支付宝，怎么注册？
**A**：可以用邮箱注册阿里云国际版，但操作会更复杂。建议有支付宝/淘宝账号的同学帮忙注册，或使用团队共用的测试 Key。

### Q：阿里云百炼的 API Key 是什么格式？
**A**：通常以 `sk-` 开头，后面跟一串字母数字，例如 `sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`。

### Q：Tavily 的 API Key 是什么格式？
**A**：通常以 `tvly-` 开头，例如 `tvly-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`。

### Q：这两个 Key 每月大概要花多少钱？
**A**：在开发测试阶段——
- 阿里云百炼：新用户免费额度通常够用几个月。即使付费，测试阶段每月最多几块钱。
- Tavily：免费计划每月 1000 次搜索，日常开发完全够用。

### Q：校内网络打不开 Tavily 怎么办？
**A**：Tavily 不是必须的。系统会自动回退使用阿里云百炼自带的联网搜索。

### Q：我可以和队友共用一个 API Key 吗？
**A**：技术上可以，但不推荐。共用 Key 会导致调用量混在一起，不容易排查问题。好在每人注册都是免费的，建议各自申请各自的。

---

## 📞 获取帮助

如果以上步骤都做了但还是跑不起来：

1. **先检查**：`.env` 里的 Key 确认不是 `your_xxx_key_here` 这样的占位符
2. **截图**：把终端里的错误信息截图
3. **发到群里**：在项目群里描述你遇到的问题并附上截图，**确保截图里不包含 API Key**
4. **联系技术负责人**：负责技术/AI 方向的同学
