[build-system]
requires = ["setuptools>=83.0"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "yunguanxingchuan"
version = "1.0.0"
description = "云观星传"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["src", "api", "config", "scripts"]
